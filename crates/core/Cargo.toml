[package]
name = "cyberfiles-core"
description = "Shared constants and config for CyberFiles"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
anyhow.workspace = true
directories = "5"
serde = { workspace = true, features = ["derive"] }
serde_json = "1"
